# The reproducible build runs these inside a Linux container: a CRLF
# checkout on Windows would break the shebang and change the bytes the
# container sees.
*.sh text eol=lf
Dockerfile text eol=lf
# Read as a git revision, and a trailing carriage return is not one.
*.rev text eol=lf
